default:
    - img
    - prod

dev:
    - 'concurrent:preBuild'
    - 'concurrent:devBuild'
    - 'watch'

prod:
    - 'concurrent:preBuild'
    - 'concurrent:devBuild'

img:
    - 'concurrent:imgMin'

devmin:
    - img
    - dev
