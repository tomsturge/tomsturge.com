default:
    - img
    - prod

dev:
    - 'concurrent:preBuild'
    - 'concurrent:devBuild'
    - 'watch'

prod:
    - 'shell:bowerupdate'
    - 'concurrent:preBuild'
    - 'concurrent:devBuild'
    - 'concurrent:prodBuild'

img:
    - 'concurrent:imgMin'

devmin:
    - img
    - dev
