default:
    - img
    - prod

dev:
    - 'concurrent:preBuild'
    - 'concurrent:devBuild'
    - 'watch'

prod:
    - 'concurrent:preBuild'
    - 'concurrent:devBuild'
    - 'concurrent:prodBuild'

img:
    - 'concurrent:imgMin'

devmin:
    - img
    - dev
