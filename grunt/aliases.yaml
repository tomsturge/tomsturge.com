default:
    - img
    - prod

dev:
    - 'concurrent:preBuild'
    - 'concurrent:devBuild'

prod:
    - 'concurrent:preBuild'
    - 'concurrent:devBuild'
    - 'concurrent:prodBuild'

bower:
    - 'shell:bowerupdate'

img:
    - 'concurrent:imgMin'

devmin:
    - img
    - dev
    
